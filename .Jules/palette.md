## 2024-05-22 - Icon-Only Buttons Accessibility
**Learning:** Icon-only buttons (like "Back" or "Send") are invisible to screen readers without an `aria-label`, creating a critical accessibility barrier.
**Action:** Always include `aria-label` describing the action for any button that lacks visible text. Add tooltips for sighted users to clarify the icon's meaning.
