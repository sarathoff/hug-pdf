# Palette's Journal

## 2024-05-22 - Accessibility in Editor Interface
**Learning:** Complex editor interfaces often neglect basic accessibility for icon-only buttons and mode toggles, making them unusable for screen reader users.
**Action:** Always verify that icon-only buttons have `aria-label` and toggle buttons use `aria-pressed` to communicate state.
